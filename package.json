{
  "name": "iris",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python -m uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
